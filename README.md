# Idiot-Game

Implementation of solo-player IDIOT. The entire project lives in the front-end. See the rules for the game [here](./docs/DesignDoc.md#rules). 

## Setup
This project compiles typescript files into javascript using `tsc`.

To host locally, run 
```bash
python -m http.server
```
and navigate to `http://localhost:8000/`.